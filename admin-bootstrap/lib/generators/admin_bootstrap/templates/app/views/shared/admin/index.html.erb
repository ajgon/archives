<div class="row-fluid">
    <table class="table table-bordered table-striped table-data" data-name="<%= @model.to_s %>">
        <thead>
            <tr>
<% @model.columns(:admin => true).each do |column| %>
                <th>
                    <a href="#" rel="tooltip" title="<%= column.type %>"><%= column.name.humanize %></a>
                </th>
<% end %>
                <th></th>
            </tr>
        </thead>
        <tbody></tbody>
        <tfoot>
            <tr>
<% @model.columns(:admin => true).each do |column| %>
                  <th>
                    <a href="#" rel="tooltip" title="<%= column.type %>"><%= column.name.humanize %></a>
                  </th>
<% end %>
              <th></th>
            </tr>
        </tfoot>
    </table>
</div>